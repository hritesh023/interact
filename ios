# iOS Configuration for Interact App

This directory would contain iOS-specific configuration for Capacitor.

To add iOS support:
1. Run: `npx cap add ios`
2. Configure iOS settings in Xcode
3. Build with: `npx cap build ios`
4. Run on device/simulator: `npx cap run ios`

Current status: iOS platform not yet added to this project.
